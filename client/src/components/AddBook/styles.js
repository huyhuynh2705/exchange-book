import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
    paper: {
        padding: '15px',
        width: '500px',
        justifyContent: 'center',
        display: 'flex',
        flexWrap: 'wrap',
    },
    form: {
        flexWrap: 'wrap',
        justifyContent: 'center',
        padding: '10px',
      },

}));

